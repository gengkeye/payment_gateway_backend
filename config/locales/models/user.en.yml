en:
  activerecord:
    attributes:
      user:
        enum:
          updates_email_subscription_level:
            never: 'Never'
            regular: 'Regular'
            important: 'Important'
            critical: 'Critical'
